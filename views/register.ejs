<!DOCTYPE html>


<div style="background-color: #dfe8fd;">
    <%- include("partials/header") -%>
    <div class="card" style="background-color: #dfe8fd;">
<title>Mentee Registration Form</title>
<link rel="stylesheet" href="css\styles.css">

<body>
<div class="container">
<h1>Student Details Registration Form</h1>
<div class="form-wrapper">
<form action="/sign_up" method="post" >

     <input type="text" class="box" id="name" name="name" placeholder="Full Name" required />
     <br>
     <input type="text" class="box" id="email" name="email" placeholder="Email" required />
     <br>
     <input type="text" class="box" id="phno" name="phno" placeholder="Mobile" required />
     <br>
     <input type="number" step="Any" class="box" id="cgpa" name="cgpa" placeholder="CGPA" required />
     <br>
     <div class="form-item">
         <label for="department">Department:</label>
         </div>
         <div class="form-item">
         <select name="department" id="department" required>
         <option value="electrical">Computer Engineering</option>
         <option value="electrical">Information Technology</option>
         <option value="electrical">ENTC</option>
         </select>
     </div>
     <br>
     <div class="form-item">
         <label for="gender">Gender:</label>
         </div>

         <div class="form-item">
           <select name="gender" id="gender" required>
           <option value="sex">Male</option>
           <option value="sex">Female</option>
           <option value="sex">Other</option>
           </select>
     </div>
     </div>

     <div class="form-item"
     <label for ="Resume">Your Resume:</label>
     <input type='file' name="resume" value="" required><br>
     </div>
     <div class="form-item"
     <label for ="id">Your Linkedin ID:</label>
     <input type='url' name="ID" value="" required><br>
     </div>

<input type="checkbox" name="" value="">
<hr>
          <button class="register" formaction="mailto:nikita.raut@cumminscollege.in">send email</button>
          <button class="register">Register</button>
</form>
</div>
</div>

</body>

<script>


        function validateForm()
        {
            var returnval=true;
            var fname=document.myform.name.value;
            if (fname==null || fname==""){
            alert("First name can't be blank");
            return false;
            }

            else if(fname.length<2)
            {
            alert("Length of first name is too short");
            return false;
            }


           var x=document.myform.Emailid.value;
           var atposition=x.indexOf("@");
           var dotposition=x.lastIndexOf(".");
           if (x==null || x==""){
            alert("email can't be blank");
            return false;
            }
            else if (atposition<1 || dotposition<atposition+2 || dotposition+2>=x.length)
            {
               alert("Please enter a valid e-mail address \n atpostion:"+atposition+"\n dotposition:"+dotposition);
               return false;
            }

            var phno=document.myform.phonenumber.value;
            if (phno==null || phno==""){
            alert("Phone number can't be blank");
            return false;
            }

            else if(phno.length<10 || phno.length>10)
            {
            alert("Phone number should have 10 digits");
            return false;
            }
            var achievements=document.myform.Yourachievements.value;
            if (achievements==null || achievements==""){
            alert(" achievements can't be blank");
            return false;
            }

            else if(achievements.length<5)
            {
            alert("words should be greater than 50");
            return false;
            }






    }



     </script>



  <br>
